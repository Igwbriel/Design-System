{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "turbo": "^1.11.3",
    "vite": "^5.0.12"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },

  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
